<!DOCTYPE html>
<html>
<head>
  <title>App</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="navbar">
  	<div class="navbar-inner">
      <div class="container">
        <a href="#" class="brand">Diary</a>
        <div class="nav-collapse">
        	<ul class="nav">
        		<li>
              <%= link_to "Home", root_path %>
        		</li>
        		<li>
              <%= link_to "Browse", diaries_path %>
        		</li>
            <li>
              <%= link_to "Add Note", new_diary_path %>
            </li>
        	</ul>
        </div>
      </div>
  	</div>
  </div>

  <% if notice %>
    <p class="notice"><%= notice %></p>
  <% end %>
  <% if alert %>
    <p class="alert"><%= alert %></p> 
  <% end %>
<%= yield %>

</body>
</html>
